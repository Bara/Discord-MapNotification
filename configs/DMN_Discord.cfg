"Discord"
{
    // This web-hook uses, if core can't find required web-hook, and if module allows use default web-hook.
    "default"       "<url>"

    // discord_MapNotification.smx
    //
    // Web-hook for sending map notifications.
    "MapNotification"        "<url>"

}
